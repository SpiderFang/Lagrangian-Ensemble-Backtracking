# 專案開發規範

## 語言與註解

- 新增或修改任何程式模組、公開類別、公開函式、重要常數、非直觀演算法與科學計算流程時，必須提供詳細、正確的繁體中文 docstring 或註解。
- 註解需說明設計原因、資料來源、欄位或座標軸意義、單位、輸入輸出、缺值與遮罩政策、物理假設及限制；不可只逐字翻譯程式碼。
- 修改行為、參數、回傳值、例外或副作用時，必須同步更新相關 docstring、README 與資料契約。
- 顯而易見的單行操作不需冗長註解；註解應集中於科學語意、數值方法與維護風險。

## 科學與資料契約

- 正式流程只能讀取 OCM schema 3 `ocm_native` 與 NWW3 schema 1 `nww3_analysis` 的已驗收產品；不得回讀或修改 raw NetCDF、transfer archive。
- 缺值、陸地、乾點、域外、海底以下、時間缺口與數值失敗必須使用不同狀態保存，不得以零值替代。
- 所有物理計算在公尺制運算座標進行；經緯度只作資料交換與圖面顯示。
- 逆向隨機系集結果一律稱為「條件式來源足跡」或「相對來源權重」。未建立先驗、似然與觀測驗證前，不得稱為絕對來源機率或因果歸因。
- 每個正式 run 必須保存完整設定、輸入 manifest、Git commit、dirty flag、seed、輸出 checksum、QC 與資源使用資訊。

## 工程與驗證

- 正式管線必須由 CLI 重建；Notebook 只能用於探索，不得成為唯一實作。
- 先建立純 NumPy 參考核心與解析／統計測試，再加入 Numba 加速；兩者必須在明定誤差內一致。
- 確定性平流與隨機擴散分開驗證。隨機位移不得插入 RK4 的 stage。
- 任何未登錄的跨月、跨網格或跨資料缺口外插均禁止。已知缺時只能由版本化重建方法處理，
  且必須保存 observed/reconstructed provenance、blocked cross-validation、Lagrangian
  sensitivity 與不確定性；未通過時改採 gap-safe arrival window，不得以最近值或零值補齊。
- 完成修改前必須檢查測試、文件、繁體中文註解、schema 與 README 是否一致。

## 專案維護

- `README.md` 為專案入口，架構、安裝、輸入輸出、狀態或執行方式改變時必須同步更新。
- Git commit message 使用中文，並清楚指出影響的模組、資料契約或方法。
- 大型資料、軌跡、server path、密碼、token 與私密現場資料不得提交至 Git。
